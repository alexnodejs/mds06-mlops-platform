apiVersion: v1
kind: Service
metadata:
  name: react-app
spec:
  type: ClusterIP
  selector:
    app: react-app
  ports:
    - name: http
      port: 80 # порт Service — звичний 80
      targetPort: http # а в контейнері це 8080 (див. ім'я порту в Deployment)
      protocol: TCP
