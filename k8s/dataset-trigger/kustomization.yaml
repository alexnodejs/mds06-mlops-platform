apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mlflow

# ⚠️ namespace.yaml тут немає навмисно: namespace mlflow оголошує застосунок
# drift-exporter, а другий власник того самого обʼєкта тримав би цей
# Application вічно OutOfSync. Сам namespace створює CreateNamespace=true.
resources:
  - reconciler.yaml
